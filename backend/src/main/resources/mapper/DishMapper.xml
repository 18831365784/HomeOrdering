<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homeordering.mapper.DishMapper">

    <resultMap id="DishResultMap" type="com.homeordering.entity.Dish">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="imageUrl" column="image_url"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="category" column="category"/>
        <result property="orderCount" column="order_count"/>
        <result property="status" column="status"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.homeordering.entity.Dish" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish (name, image_url, description, price, category, order_count, status, sort, create_time, update_time)
        VALUES (#{name}, #{imageUrl}, #{description}, #{price}, #{category}, 0, #{status}, #{sort}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectById" resultMap="DishResultMap">
        SELECT * FROM dish WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="DishResultMap">
        SELECT * FROM dish
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort ASC, create_time DESC
    </select>

    <update id="update" parameterType="com.homeordering.entity.Dish">
        UPDATE dish
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="category != null">category = #{category},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM dish WHERE id = #{id}
    </delete>

    <update id="increaseOrderCount">
        UPDATE dish SET order_count = order_count + #{count}
        WHERE id = #{dishId}
    </update>

</mapper>
