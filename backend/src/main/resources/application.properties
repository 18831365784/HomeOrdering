# 服务器配置
server.port=8080
server.servlet.context-path=/api
# 服务器完整URL地址
# 本地开发时使用:
#server.url=http://localhost:8080
# 真机调试时，配置为你的内网穿透域名(注意不要添加端口号):
server.url=https://unperverted-neida-noncounterfeit.ngrok-free.dev

# 数据源配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://172.16.1.134:3306/home_ordering?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
spring.datasource.username=root
spring.datasource.password=eplugger

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.homeordering.entity
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 图片存储配置(本地存储路径，可扩展为云存储)
# 服务器地址（开发完成后可切换回服务器）
# file.upload.path=E:/Code/WeChatMiniProgram/HomeOrdering/backend/uploads/
# file.upload.type=local

# 本地开发地址
file.upload.path=./uploads/
file.upload.type=local
# 云存储配置(预留，切换为cloud时启用)
#file.upload.type=cloud
#file.upload.cloud.accessKey=
#file.upload.cloud.secretKey=
#file.upload.cloud.bucket=
#file.upload.cloud.endpoint=

# 微信小程序配置
wx.appid=wxe17c8a6b5b7ff823
wx.secret=7cda5e74ad68a823dd29aae7e2d1a229

# 日志配置
logging.level.com.homeordering=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n